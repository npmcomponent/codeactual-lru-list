{
  "name": "codeactual-lru-list-component",
  "description": "Storage agnostic LRU list",
  "version": "0.0.1",
  "devDependencies": {
    "chai": "1.5.0",
    "mocha": "1.8.1"
  },
  "scripts": {
    "test" : "mocha --async-only --reporter spec test.js"
  }
}
